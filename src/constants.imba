export const threshold = 120
export const innerWidth = window.innerWidth - threshold
export const innerHeigth = window.innerHeight - threshold
export const isDev = window.location.hostname === '127.0.0.1'
